# Completed: a slice is a path, not a layer

Lesson 0001 is done. The floor moves: do not re-teach Wake’s cake. Future sessions may retrieve it (classify a layer vs a slice) but should assume the distinction has been practised.

The next bottleneck is not “what is a slice” but “which thin path do you keep.” Ticket-guided work and coding agents both produce throwaway spikes that look like slices, and fat first tickets that look like product. That is lesson 0002.

**Evidence:** the learner reported finishing the lesson, including classify + rewrite practice. Rewritten slices were not pasted back, so depth of independent rewrite is still unverified — if a later rewrite still names a table or a notebook, treat 0001 as exposure, not mastery.
