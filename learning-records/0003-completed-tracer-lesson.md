# Completed: a tracer is kept, a prototype is thrown

Lesson 0002 is done. The floor moves: do not re-teach keep-vs-throw as if new. Future sessions may retrieve it (notebook vs product path; a stubbed model on a real UI→API→SQL path is still a tracer).

The next bottleneck is not “which thin path do you keep” but “how you verify it.” Ticket-guided work often tests after the code exists. Agents write the feature plus a bulk suite of imagined behaviour. That is lesson 0003: one failing test of the path, then only enough code to pass it.

**Evidence:** the learner reported finishing the lesson, including classify + rewrite practice. Rewritten tracers were not pasted back, so independent rewrite depth is still unverified — if a later first-test still names a notebook, a layer, or a twelve-test suite, treat 0002 as exposure, not mastery.

**Implications:** spec templates, walking skeleton, mocking strategy, and refactor-as-a-skill remain parked. The TDD loop starts at red, not at the full red-green-refactor ritual.
