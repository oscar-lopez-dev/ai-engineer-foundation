# Completed: only enough code to pass

Lesson 0004 is done. The floor moves: do not re-teach extra-feature vs cheat-the-test vs only-enough, as if new. Future sessions may retrieve it (the test file stays unchanged; the rest of the list stays notes; a stub that returns “I don’t know” is the smallest pass).

The next bottleneck is not “what you write once it is red” but “what you write once it is green.” Ticket-guided work treats one pass as permission to build the product. Agents do that, and they also turn the leftover cases into a suite. That is lesson 0005: one next failing test of the tracer you already named. Existing tests stay green. The rest of the list stays a list.

**Evidence:** the learner reported finishing the lesson, including classify + rewrite practice. Rewritten pass prompts were not pasted back, so independent rewrite depth is still unverified — if a later “next test” prompt still asks to wire OpenAI, edit the first test, or implement the list, treat 0004 as exposure, not mastery.

**Implications:** refactor remains parked. Beck treats it as optional; Fowler treats skipping it as the common TDD failure; Pocock’s `/tdd` parks it in review. The sources disagree on where tidy-up sits. Teach it after the learner can cycle red → green → next test. Fake It / Triangulate, mocking, and spec templates stay parked.
